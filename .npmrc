# Use hoisted node_modules (flat structure like npm)
# This is required for Docker multi-stage builds because
# pnpm's default symlinked node_modules breaks when copied
# between Docker stages.
node-linker=hoisted
